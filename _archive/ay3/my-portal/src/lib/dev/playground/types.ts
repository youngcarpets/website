// Re-export from the canonical lib/dev/types.ts location.
// Kept for backwards compatibility with existing playground imports.
export type {
	ShowcaseCategory,
	EditablePropType,
	EditableSection,
	EditableProp,
	PropSpec,
	ShowcaseEntry
} from '$lib/dev/types';
